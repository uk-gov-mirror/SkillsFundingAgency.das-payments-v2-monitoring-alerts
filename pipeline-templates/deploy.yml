parameters:
  pool:
  azureSubscription:
  location: 'West Europe'
  environmentName:
  appName:
  
jobs:
- deployment: DeployAzureFunctionApp
  pool:
    name: ${{ parameters.pool }}
    demands:
    - ReleaseEnvironment -equals ${{ parameters.environmentName }}
    workspace:
      clean: all
  environment: ${{ parameters.environmentName }}
  
  
  strategy:
    runOnce:
      deploy:
        steps:

        - task: AzureAppServiceSettings@1
          displayName: 'Apply Function App Settings'
          inputs:
            azureSubscription: ${{ parameters.azureSubscription }}
            appName: ${{ parameters.appName }}
            resourceGroupName: ''
            appSettings: |
               [
                { "name": "TeamsWebhookURL2", "value": "$(TeamsWebhookURL2)" }
                
               ]

        - task: AzureFunctionApp@2
          displayName: 'Azure Function App Deploy'
          inputs:
            azureSubscription: ${{ parameters.azureSubscription }}
            appType: functionApp
            appName: ${{ parameters.appName }}
            package: '$(pipeline.workspace)/MonitoringAlertsFunctionApp/Monitoring-Alerts-FunctionApp.zip'